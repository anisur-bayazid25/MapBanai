<?xml version="1.0" encoding="utf-8"?>
<!-- Paths exposed via the ${applicationId}.fileprovider FileProvider,
     covering the directories used by UpdateDownloader. -->
<paths>
    <external-path name="external_files" path="." />
    <cache-path name="cache" path="." />
</paths>
